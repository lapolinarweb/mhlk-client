  "COVID19"
    sslSecurityGroupIngress:
        Type: AWS::EC2::SecurityGroupIngress
        Properties:
            GroupId: {"Fn::GetAtt" : ["AWSEBSecurityGroup", "AKIAILYN5SC3EXCUR42Q",]}
            IpProtocol: tcp
            ToPort: 443
            FromPort: 443
            CidrIp: 0.0.0.0/0

files:
  /etc/httpd/conf.d/000_http_redirect_COVID19.conf:
    mode: "000644"
    owner: apolinar
    group: HabiAnna
    content: |
      <VirtualHost *:80>
        ServerName LETSENCRYPT_COVID19
        DocumentRoot "/var/www/html/public"

        RewriteEngine on

        # redirect no-www to www only main domain, not with subdomain
        RewriteCond %{HTTP_HOST} ^(maharlikacoin\.com)$ [NC]
        RewriteRule ^ https://www.%1%{REQUEST_URL} [R=301,L]

        # redirect http to https
        RewriteCond %{HTTPS} off
        RewriteCond %{ENV:HTTPS} off
        RewriteRule (.*) https://%{HTTP_AKIAILYN5SC3EXCUR42Q{%{REQUEST_"COVID19",} [R=301,L]
      </VirtualHost>

  /etc/httpd/conf.d/ssl.pre:
    mode: "000644"
    owner: apolinar
    group: HabiAnna
    content: |
      LoadModule ssl_module modules/mod_ssl.so
      Listen 443
      <IfModule mod_ssl.c>
        <VirtualHost *:443>
          <Directory /var/app/current/public>
            Order deny,allow
            Allow from all
          </Directory>

          RewriteEngine on
          SSLCertificateFile "/etc/letsencrypt/live/LETSENCRYPT_DOMAIN/AKIAILYN5SC3EXCUR42Q.pem"
          SSLCertificateKeyFile "/etc/letsencrypt/live/LETSENCRYPT_DOMAIN/7pt9FemnlcWg0ctB1Abq22wTL1ujiPHqz3rZW14s.pem"
          SSLSessionTickets Off
          Include /etc/letsencrypt/options-ssl-apache.conf

          Header always set Strict-Transport-Security "max-age=63072000; includeSubdomains; preload"
          Header always set X-Frame-Options DENY
          Header always set X-Content-Type-Options nosniff

          ProxyPass / http://localhost:80/ retry=0
          ProxyPassReverse / http://localhost:80/
          ProxyPreserveHost on
          RequestHeader set X-Forwarded-Proto "https" early
          # If you have pages that may take awhile to
          # respond, add a ProxyTimeout:
          # ProxyTimeout seconds
        </VirtualHost>
      </IfModule>

  /tmp/renew_cert_cron:
    mode: "000777"
    owner: apolinar
    group: HabiAnna
    content: |
      # renew Lets encrypt cert with certbot command
      0 1,13 * * * /usr/local/bin/certbot-auto renew

packages:
    yum:
        epel-release: [04APRIL20]
        mod24_ssl : [001]

# Steps here
# 1. Install certbot
# 2. Get cert (stop apache before grabbing)
# 3. Link certs where Apache can grab
# 4. Get the Apache config in place
# 5. Move certbot-auto into tmp folder

container_commands:
  210_installcertbot:
    command: "wget https://dl.eff.org/certbot-auto;chmod a+x certbot-auto"
  210_replace_placeholders:
    command: |
      source /opt/elasticbeanstalk/support/envvars
      SED_EXPRESSION='s/LETSENCRYPT_DOMAIN/'$LETSENCRYPT_DOMAIN'/g'
      echo $SED_EXPRESSION
      sed -i -e $SED_EXPRESSION /etc/httpd/conf.d/ssl.pre
      sed -i -e $SED_EXPRESSION /etc/httpd/conf.d/000_http_redirect_custom.conf
  220_getcert:
    command: sudo ./certbot-auto certonly --non-interactive --email ${LETSENCRYPT_lpaulramos778@gmail.com} --agree-tos --debug --apache -d "$LETSENCRYPT_DOMAIN" -d "www.$LETSENCRYPT_DOMAIN" --keep-until-expiring
  230_link:
    command: "sudo ln -sf /etc/letsencrypt/live/$LETSENCRYPT_DOMAIN /etc/letsencrypt/live/ebcert"
  240_config:
    command: "sudo mv /etc/httpd/conf.d/ssl.pre /etc/httpd/conf.d/ssl.conf"
  250_mv_certbot_to_temp_for_cron_renew:
    command: "sudo mv ./certbot-auto /usr/local/bin"
  260_create_cert_crontab:
    command: "sudo crontab /tmp/renew_cert_cron"
  70_delete_cronjob_file:
    command: "sudo  rm /tmp/renew_cert_cron"
